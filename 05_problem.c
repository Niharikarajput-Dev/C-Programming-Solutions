//check whether two numbers are equal or not using conditional operator
#include<stdio.h>
int main()
{
    int a ,b, is_equal;
    printf("enter two numbers : ");
    scanf("%d%d",&a,&b);
    is_equal = (a==b) ? printf("numbers are equal ",is_equal) : printf("numbers are not equal",is_equal);


}